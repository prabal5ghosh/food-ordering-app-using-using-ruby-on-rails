<!DOCTYPE html>
<html lang="en">
  <head>
    <title >Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>
    <div class="container mt-3" style="margin-left:40px">
    <h1 style="text-align:center; background-color:#b4f896; margin-bottom: 25px;height:50px; color:#ea0f0f;"> <strong> WELCOME ADMIN </strong></h1>
      <h2 class="bg-info" style="width:300px; margin-bottom: 25px;height:50px; color:yellow;"><strong> Users list: </strong></h2>        
      <table class="table table-dark table-striped" style="margin-left:10px; width:1050px;border: 1px solid;border-color:yellow;">
        <thead>
          <tr>
            <th class="bg-success" style="border:1px solid; border-color:yellow; "> user id </th>
            <th class="bg-warning" style="border:1px solid; border-color:yellow;"> user first name</th>
            <th class="bg-info" style="border:1px solid; border-color:yellow;">user last name </th>
            <th style="border:1px solid; border-color:yellow;"> email </th>
            <th class="bg-warning" style="border:1px solid;border-color:yellow;"> user role type</th>
            <th class="bg-success" style="border:1px solid;border-color:yellow;">  view  </th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user|  %>
            <tr>
            <% if user.role_id != 4  %>
              <td> <%= user.id %>  </td>
              <td> <%= user.first_name %> </td>
              <td> <%= user.last_name %> </td>
              <td> <%= user.email %> </td>
              <td> <%= user.role_id %> </td>
              <td>
             <%#>   Restaurant.find_by(user_id: User.find_by(id: 13))   %>
                      <%#>     <%= button_to "View", 'user_path' , method: "get", style:"color:#ff00de ;font-weight: bold;background-color: #f4f1b1; " ,class:"btn btn-danger"%>     
                <a class="TodoItem-delete"  href="javascript:void(0)">
                  <%= form_with(url: "/users/#{user.id}", method: "get") do %>                 
                    <button class="btn btn-success" type="submit">View</button>
                  <% end %>
                </a> 
              </td>  
            <% end %>        
                
            </tr>          
          <% end %>
        </tbody>
      </table>
    </div>
  </body>
</html>
