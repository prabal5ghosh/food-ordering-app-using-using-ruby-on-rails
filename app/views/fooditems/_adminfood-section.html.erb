<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Bootstrap Example</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <div class="container mt-3" >
            <h2 style="color:pink;background-color:;margin-top: 200px;"> Food Item list</h2>        
            <table class="table table-dark table-striped" style="margin-left:10px; width:900px;border-color:#00f6da;">
                <thead>
                    <tr>
                        <th class="bg-success"><strong> Food Name </strong> </th>
                        <th class="bg-warning"> <strong>price</strong></th>
                        <th class="bg-success"> <strong> VEG or NonVeg</strong></th>
                        <th class="bg-warning"> <strong>catagory</strong></th>
                        <th class="bg-info" >  <strong>restaurant name</strong> </th>
                        <th class="bg-danger"> <strong> View foods_restaurant: </strong> </th>
                    </tr>
                </thead>
                <tbody>
                    <% @fooditems.each do |fooditem|  %>
                        <tr>
                            <td> <%= fooditem.food_name %> </td>
                            <td> <%= fooditem.price %> </td>
                            <td> 
                                <% if fooditem.veg_food == true  %>
                                    Veg
                                <% else %>
                                    NonVeg 
                                <% end %>
                                </td>
                            <td>                
                                <% if fooditem.category_id == 1  %>
                                    Bengali Food
                                <% elsif fooditem.category_id == 2 %>
                                    North Indian Food
                                <% elsif fooditem.category_id == 3 %>
                                    South Indain Food
                                <% else %>
                                    Russian Food
                                <% end %>       
                            </td>
                            <td>
                                <%= Restaurant.find_by(id:@restaurant.id).restaurant_name %>
                            </td>
                            <td class="text-center"> 
                                <%= button_to "View", "/users/#{@restaurant.user_id}/restaurants/#{@restaurant.id}/fooditems/#{fooditem.id}" , method: "get", style:"color:#ff00de ;font-weight: bold;background-color: #f4f1b1; " ,class:"btn btn-danger"%>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
        <div class="text-center">
            <p> <strong> <%= link_to 'Back', user_restaurants_path %> </strong> <p>
        <div>
    </body>
</html>